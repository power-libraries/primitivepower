package com.github.powerlibraries.primitive.common;

import static org.assertj.core.api.Assertions.assertThat;

import java.util.Random;

import org.junit.jupiter.api.Test;

public class Default{{t.label}}PointerTest {

	@Test
	public void test() {
		Default{{t.label}}Pointer pointer = new Default{{t.label}}Pointer();
		
		Random r = new Random(7);
		
		for(int i=0; i<100; i++) {
			{{t.type}} v = {{t.randomValue('r')}};
			pointer.set(v);
			assertThat(pointer.get()).isEqualTo(v);
		}
	}
}